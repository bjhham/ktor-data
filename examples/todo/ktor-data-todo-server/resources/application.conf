ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [
      io.ktor.data.todo.server.HttpKt.configureHttp
      io.ktor.data.todo.server.DatabaseKt.configureDatabase
      io.ktor.data.todo.server.RoutingKt.configureRouting
    ]
  }
}

database {
  url = "r2dbc:h2:file:///./h2/db"
  user = root
  driver = org.h2.Driver
  password = ""
}
